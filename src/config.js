// config.js
const config = {
    apiUrls: {
      carteira: 'http://localhost:3001/api', // URL para a API de carteira
      rendaFixa: 'http://localhost:3002/api', // URL para a API de renda fixa
    },
  };
  
  export default config;